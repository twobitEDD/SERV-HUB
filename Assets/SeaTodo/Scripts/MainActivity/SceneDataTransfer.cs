using System;

namespace MainActivity
{
    // Save params out of scenes
    public static class SceneDataTransfer
    {
        // Get data firstly when launch app (do it in logo for optimization)
        public static DateTime CurrentData;
    }
}
